#include <stdio.h>
#include "SqList.h"

void SearchExchangeInsert(SqList &L, ElemType x)
{
    for (int i = 0; i < L.length; i++)
    {
        if (L.data[i] < x)
        {
            
        }
        
    }
    
}

int main()
{
    printf("hello");

    return 0;
}